server:
  port:  7777
  servlet:
    contextPath:  /uaa #web基路径
spring:
  application:
    name:  auth-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true